sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payment
        config:
          loaded_at_field: _batched_at
          freshness:
            error_after:
              count: 14
              period: day
            warn_after:
              count: 7
              period: day
