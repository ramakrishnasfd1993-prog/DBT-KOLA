version: 2

sources:
  - name: jaffle_shop
    database: raw
    schema: PRACTICE_RAW
    tables:
      - name: students
      - name: college
      - name : branch
        config:
         loaded_at_field: _BATCHED_AT
         freshness: 
          error_after:
            count: 12
            period: hour
          warn_after:
            count: 5
            period: hour
        

